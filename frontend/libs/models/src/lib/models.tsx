import React from 'react';

import './models.css';

/* eslint-disable-next-line */
export interface ModelsProps {}

export const Models = (props: ModelsProps) => {
  return (
    <div>
      <h1>Welcome to models component!</h1>
    </div>
  );
};

export default Models;
