import React from 'react';

import './services.css';

/* eslint-disable-next-line */
export interface ServicesProps {}

export const Services = (props: ServicesProps) => {
  return (
    <div>
      <h1>Welcome to services component!</h1>
    </div>
  );
};

export default Services;
